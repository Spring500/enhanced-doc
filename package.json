{
  "name": "enhanced-doc",
  "version": "2.1.0",
  "description": "Self-injecting markdown rendering engine with Mermaid, ECharts, MathJax",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "playwright test",
    "test:all": "vitest run && playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.52.0",
    "jsdom": "^29.1.1",
    "vitest": "^3.1.0"
  }
}
